- flash.each do |key, value|
  = content_tag(:div, value, class: "flash flash__#{key}")
  -# - if key == "nortice"
  -#   = content_tag(:div, value, class: "#{key} alert-success")
  -# - elsif key == "alert"
  -#   = content_tag(:div, value, class: "#{key} alert-danger")
  -# - else
  -#   = content_tag(:div, value, class: "#{key}")
